<?xml version="1.0" encoding="utf-8"?>
<!-- Widget görünümü: uygulamanın koyu zemini, formül adı ve durum satırı. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/plasebo_widget_bg"
    android:padding="14dp">

    <TextView
        android:id="@+id/widget_streak"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#7B6EF6"
        android:textSize="11sp"
        android:maxLines="1"
        android:text="" />

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#FFFFFF"
        android:textSize="17sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="@string/widget_default_title" />

    <TextView
        android:id="@+id/widget_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:textColor="#C5C2B8"
        android:textSize="12sp"
        android:maxLines="2"
        android:ellipsize="end"
        android:text="@string/widget_default_subtitle" />
</LinearLayout>
